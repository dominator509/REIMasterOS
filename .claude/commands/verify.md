# /verify — Run full verification suite

#

# Usage: /verify [quick|full|preflight]

#

# quick — format + lint + typecheck

# full — all checks including tests, build, security, audit

# preflight — preflight only

Run the verification commands from COMMANDS.md.
Report pass/fail for each check. On failure, diagnose and fix before re-running.
