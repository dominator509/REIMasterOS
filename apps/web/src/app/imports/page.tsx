export default function ImportsPage() {
  return (
    <div>
      <h1>Import Data</h1>
      <p>Import properties, leads, and contacts from CSV files.</p>
      <div
        style={{
          background: "#fff",
          borderRadius: 8,
          padding: "2rem",
          textAlign: "center",
          color: "#999",
        }}
      >
        CSV import will be available here. Upload a CSV file to bulk-import property and lead data.
      </div>
    </div>
  );
}
